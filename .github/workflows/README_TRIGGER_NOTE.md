This directory includes a manual `workflow_dispatch` recovery path in `release.yml` for republishing an existing semantic-version tag to GHCR when a tag-triggered publish event was missed. The recovery validates the requested tag against `pyproject.toml` and `CHANGELOG.md`, reruns the release validation surface, and republishes only the container package; it does not create a new release version.
